# 更新说明（Changelog）

## v1.1.0（2026-08-14）

### 🔧 修复

- **引擎只处理已收盘的 1 小时 K 线，且每根 K 线只处理一次**（`barstate.isconfirmed` + `bar_index` 去重）。修复实时状态下引擎每个 tick 重复执行导致的三个问题：
  - 正在形成的 1H K 线证据被逐 tick 重复累加，分数组件迅速饱和，最近一小时的动作垄断整张热力图；
  - 每日开盘第一小时 `newTradingDay` 逐 tick 为真，每日衰减被触发数百上千次，历史证据在开盘一小时内被清零；
  - 形成中 K 线的影线极值不断变化，造成区间中心随 tick 漂移重绘。
- 修复首根 K 线 `ta.change(time("D")) != 0` 的 na 误判（Pine 中 `na != 0` 为真），改为 `> 0`。
- 修复后价位与强度只在 1 小时 K 线收盘后更新；历史回填行为不变，正在形成的 1H K 线事件滞后到收盘后计入（最多约 1 小时）。

### ✨ 新增

- **最小显示间距（ATR）**：核心设置，默认 1.25（设为 0 禁用）。与已显示区间中心距离小于 `ATR × 间距` 的候选区间被跳过，只保留分数更高者，避免相邻价位互相干扰。
- **标签字号**：显示设置，小 / 中 / 大，默认“中”（比原先的 tiny 大一号）；标签底色更不透明，数字更清晰。

### 🧹 清理

- 删除脚本头部与 README 中重复三遍的警告段落。
- 同步更新脚本内部的数据与重绘声明，与已收盘 K 线的新行为一致。

## v1.0.0

- 初始版本：1 小时关键价位热力图。
- 五类证据加权（触及与拒绝 / 多空转换 / 相对成交量 / 缺口 / 整数共振）、短期与长期双重日衰减、上下方各半名额分配、按 ATR 合并相邻微区间、热力区间与分数标签绘制。
